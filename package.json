{
  "name": "chrismas-app",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm run prod --prefix frontend && mv backend/dist ."
  },
  "cacheDirectories": ["frontend/node_modules"]
}